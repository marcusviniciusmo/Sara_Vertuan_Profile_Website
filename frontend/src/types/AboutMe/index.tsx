export interface AboutMePictureProps {
  cover: string;
  theme: string;
};

export interface AboutMeProps {
  language: string;
  strong: string;
  text: string;
};

export interface AboutMeSpanProps {
  theme: string;
};